const menuBtn=document.querySelector('.menu-btn');const nav=document.querySelector('.site-header nav');if(menuBtn){menuBtn.addEventListener('click',()=>nav.classList.toggle('open'));nav.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')))}function demoSubmit(e){e.preventDefault();document.getElementById('form-msg').textContent='Demo message submitted locally — no real message was sent.';e.target.reset();return false}